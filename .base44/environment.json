{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "STRIPE_SECRET_KEY",
      "description": "Stripe secret API key from Stripe Dashboard → Developers → API keys. Required at API startup; placeholder is used for boot without real payments.",
      "requiredAtBoot": false
    },
    {
      "name": "RESEND_API_KEY",
      "description": "Resend API key for transactional emails from Resend Dashboard → API Keys. Required at API startup; placeholder is used for boot without email sending.",
      "requiredAtBoot": false
    }
  ]
}
