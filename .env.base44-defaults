# Base44 dev defaults — placeholders so the app boots without real credentials.
# Real secrets (when provided via dashboard) override these because
# /run/base44/app.env is listed AFTER this file in compose env_file.

JWT_SECRET=base44-dev-jwt-secret-placeholder
ADMIN_PASSWORD=base44-dev-admin-password
STRIPE_SECRET_KEY=sk_test_placeholder_not_a_real_key
NODE_ENV=development
LOG_LEVEL=info
RESEND_API_KEY=re_placeholder_not_a_real_key
FRONTEND_URL=https://3000-${BASE44_PUBLIC_HOST_SUFFIX}
